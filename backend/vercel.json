{
  "version": 2,
  "builds": [
    {
      "src": "api/subscribe.py",
      "use": "@vercel/python",
      "config": {
        "maxLambdaSize": "50mb"
      }
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/subscribe.py" }
  ]
}
